---
title: Code Highlighting in Rmarkdown
author: Rob Wiederstein
date: '2021-02-14'
slug: try-syntax-highlighting
categories:
  - R
tags:
  - R Markdown
description: A blog post on coding must use code highlighting to make it easier on readers to grasp key concepts.
images:
  - 2021/02/14/try-syntax-highlighting/stairwell.jpg
output:
  blogdown::html_page:
    toc: true
---

```{r load-libraries, include=F}
library(ggplot2)
```

# Example 1

```{r example-syntax-highting}
# Dr Seuss
a <- 1:2
fish.number <- paste(a, "fish", sep ="_")
b <- c("red", "blue")
fish.color <- paste(b, "fish", sep = "_")
print(c(fish.number, fish.color))
```

# Example 2

```{r plot-mtcars, fig.align="center", fig.cap="A plot constructed with `ggplot2`."}
data(mtcars)
p <- ggplot(mtcars, aes(hp,mpg, group = factor(cyl), color = factor(cyl)))
p <- p + geom_point()
p <- p + ggtitle("Mtcars")
p
```
